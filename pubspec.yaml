name: anywhere_list_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  http: ^1.1.0
  get_it: ^7.6.4
  flutter_hooks: ^0.20.3
  mockito: ^5.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:
  assets:
    - assets/error_image.png
  fonts:
    - family: Simpsonsfont
      fonts:
        - asset: assets/simpsons_font.ttf


  uses-material-design: true
